[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests that call the live API server (use -m integration to run)
    slow: marks tests that call real LLM APIs (subset of integration)
addopts = -v --tb=short -m "not integration"
